server:
  port: 8080
  
eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      Instance:
        preferIpAddress: true
       
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        - id: animals-service-route
          uri: lb://animals-service
          predicates:
            - Path=/games/**
        - id: animals-library-service-route
          uri: lb://animals-library-service
          predicates:
            - Path=/library/**
